void main() {
  // late ToolLocalDataSourceImpl toolLocalDataSourceImpl;

  // setUp(() {
  //   toolLocalDataSourceImpl = ToolLocalDataSourceImpl();
  // });

  // const tTools = [ToolModel(title: '', body: '', icon: '')];

  // test('should return list of tools', () async {
  //   when(() => rootBundle.loadString(any()))
  //       .thenAnswer((_) async => readJson(AppJsonTest.toolResult));

  //   final result = await toolLocalDataSourceImpl.getTools();

  //   expect(result, equals(tTools));
  // });
}
